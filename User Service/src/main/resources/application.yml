server:
  port: 8081
spring:
  application:
    name: user-service # 服务名称（消费者将通过此名称调用
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ #注册到Eureka Server地址
    instances:
      prefer-ip-address: true #注册时用ip
